class ChatItemModel {
  final String userName;
  final String lastMessage;
  final String imageUrl;
  final DateTime dateTime;

  ChatItemModel(this.userName, this.lastMessage, this.imageUrl, this.dateTime);
}
